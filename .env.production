# Production build (Vite + Vercel).
# Use same-origin "/api" so the browser never hits cross-origin CORS.
# vercel.json proxies /api and /uploads to the Render backend.
VITE_API_BASE_URL=/api

# Never enable skip-payment in production.
VITE_ALLOW_DEV_SKIP_PAYMENT=false
